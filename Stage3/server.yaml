apiVersion: v1
kind: Pod
metadata:
  name: server
spec:
  containers:
    - image: javaderek/greeter:stage2
      name: server
      ports:
        - containerPort: 8080
          name: http
          protocol: TCP